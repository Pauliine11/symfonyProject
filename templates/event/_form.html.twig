
{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
            {{ form_errors(form) }}
    </div>
    {{ form_widget(form) }}
    <div class="d-flex justify-content-end">
        <button class="btn btn-dark mt-2">{{ button_label|default('Créer') }}</button>
    </div>
{{ form_end(form) }}