{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center mt-5">Quel sera le prochain enlève..évènement ?</h1>
    <div class="container log register text-light mt-5">
        {{ include('event/_form.html.twig') }}
    </div>
    {# <a href="{{ path('app_event_index') }}">back to list</a> #}
{% endblock %}

